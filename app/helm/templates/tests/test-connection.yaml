apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-data-pipeline.fullname" . }}-test-connection"
  labels:
    {{- include "event-data-pipeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-data-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
