apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-data-pipeline.fullname" . }}
  labels:
    {{- include "event-data-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-data-pipeline.selectorLabels" . | nindent 4 }}
